# Guitar IR Analyzer V2.3

[![Streamlit App](https://static.streamlit.io/badges/streamlit_badge_black_white.svg)](https://ir-analyzer.streamlit.app/)
![Python](https://img.shields.io/badge/python-3.9%2B-blue)
![License](https://img.shields.io/badge/license-MIT-green)

**Guitar IR Analyzer** は、ギターキャビネットのインパルス応答（IR: Impulse Response）ファイルを解析するためのWebアプリケーションです。



🔗 **Live Demo:** [https://ir-analyzer.streamlit.app/](https://ir-analyzer.streamlit.app/)

---

## 目次

- [特徴](#特徴)
- [インストールと実行](#インストールと実行)
- [使い方](#使い方)
- [使用ライブラリ](#使用ライブラリ)
- [開発者情報](#開発者情報)

---

## 特徴

このアプリケーションは、キャビネットIRファイルをエンジニアやミュージシャンが簡単かつ詳細に解析・比較できるようにすることを目的に作成しました。

### 1. 波形解析 (Time Domain Analysis)

#### 規格化モードの選択
* **Relative Normalization（相対規格化）**:
    * 各ファイルを個別に最大値=1.0へ規格化し、波形の**形状**に焦点を当てた比較を行います
    * 異なるマイク距離やゲイン設定で録音されたIRの「トランジェント特性」や「減衰カーブ」を視覚的に比較する際に有効です
    
* **Absolute Normalization（絶対規格化）**:
    * 全ファイルの最大振幅を基準に規格化することで、ファイル間の**実効的な音量差**を保持します
    * 「マイクを1cm遠ざけた時のレベル低下」や「プリアンプゲインの違いによる影響」を定量的に観測可能です
    * マイクロフォン配置の最適化や、複数のキャビネット間の音圧レベル比較に有用です

#### Peak Alignment（ピークアライメント）
* ワンクリックで全波形のピーク位置（時間軸）を揃えることができます
* 異なる録音タイミングで得られたIR間の「初期反射の立ち上がり特性」や「位相関係」を正確に比較するために使用します
* 特に、同一セットアップで複数回録音した際の再現性評価や、異なるマイクポジションでの時間差補正に効果的です

#### 可変表示範囲
* 1ms～100msの範囲で時間軸のズーム表示が可能です
* 初期反射（Early Reflection）やトランジェント成分を詳細に観察できます

---

### 2. 周波数解析 (Frequency Response Analysis)

#### 窓関数の選択
解析目的に応じて、以下の窓関数から最適なものを選択できます。各窓関数は、**周波数分解能**と**スペクトル漏れ（サイドローブ）の抑制**のトレードオフを持っています。デフォルトではHann窓が選択されます。

* **Rectangular (Boxcar)**:
    * 最も高い周波数分解能を持つが、サイドローブが大きい
    * 単一周波数成分の測定や、厳密な周波数分解能が必要な場合に使用
    
* **Hann**:
    * 分解能とサイドローブ抑制のバランスが良い汎用的な窓関数
    * 一般的なIR解析では、これらを推奨します

* **Hamming**:
    * メインローブがHannよりわずかに狭い（周波数分解能がわずかに良い）
    * より強いサイドローブ抑制が必要な場合に有効
    
* **Blackman / Blackman-Harris**:
    * 高いサイドローブ抑制能力を持つが、メインローブが広がる
    * 複数の周波数成分が近接している場合や、微小な成分を検出したい場合に有効
    
* **Kaiser （β=5.0 / 8.6 / 14.0）**:
    * βパラメータでサイドローブ抑制レベルを調整可能
    * β=5.0: 高分解能（Hannに近い特性）
    * β=8.6: バランス型（一般的な測定用途）
    * β=14.0: 高精度・低サイドローブ（精密測定用）
    
* **Tukey （α=0.1 / 0.5 / 0.9）**:
    * αパラメータでテーパー（減衰）部分の割合を制御
    * α=0.1: 矩形窓に近い（高分解能）
    * α=0.5: Hann窓とRectangularの中間
    * α=0.9: Hann窓に近い特性
    
* **Flat Top**:
    * 振幅精度が極めて高い（±0.01 dB程度）
    * 周波数分解能は低いが、**ゲイン測定**や**キャリブレーション**に最適

* **Bartlett**:
    * 三角窓。Hann窓より若干サイドローブが大きいが、計算が軽量

#### Data Shift Mode（データシフト機能）
Bell型窓関数（Hann、Kaiser等）を使用する際の重要な最適化機能です。

**背景:**  
一般的なIRファイルでは、インパルス成分が$t=0$付近に存在します。Hann窓などのBell型窓関数は、データの両端（$t=0$および$t=T$）でゼロに減衰する特性を持つため、**最も重要なインパルス成分が窓関数の減衰域にかかってしまい、S/N比が劣化**してしまいます。

**解決策:**  
本ツールのData Shift Modeは、FFT前にデータを**循環シフト**（**circular shift**）させることで、インパルスのピークを窓関数の中心（最大値位置）へ移動させます。これにより、インパルス成分が窓の減衰域にかからず、**正確な周波数応答とS/N比の向上**が実現されます。

* **Auto Mode（推奨）**:
    * Bell型窓関数が選択されている場合のみ、自動的にシフトを適用します
    * 矩形窓では不要なシフトを回避します
    
* **None**:
    * シフトを行わない（生データをそのまま使用）
    
* **Circular Shift**:
    * データの先頭部分を中心へ循環移動させます
    
* **Peak Centering**:
    * ピーク位置を自動検出し、窓の中心へ移動させます（最も精密）

#### オクターブスムージング
* 1/48オクターブから1オクターブまでの可変スムージング処理に対応
* **聴感上の特性**に近い表示が可能です
* 細かなスペクトルの凹凸を平滑化し、全体的な周波数特性の傾向を把握しやすくします

#### FFT設定の最適化
* **ゼロパディング（Zero-padding）**:
    * 短いIRファイルでも、指定したFFT Durationに基づいて自動的にゼロパディングを行い、十分な周波数分解能を確保します
    
* **FFTサイズの手動指定**:
    * 2048～65536の範囲で最小FFTサイズを指定可能
    * より高い周波数分解能が必要な場合は、大きな値を設定してください

---

### 3. エクスポート機能
Streamlitの仕様上、ブラウザ画面上に表示できる画像の解像度に制限があるため、別途エクスポート機能を実装しています。

**PNG**、**JPG**、**SVG**、**TIF**、**PDF**の中から、用途に応じた最適な画像フォーマットを選択できます。また、必要に応じて最大**2000 DPI**まで解像度を選択できます。 

#### 視認性のカスタマイズ
* **テーマ設定**: ダークモード / ライトモード対応
* **グリッド調整**: 線の太さ、透明度、色を細かく設定可能
* **テキストサイズ**: 軸ラベルや凡例のフォントサイズを調整
* **配色パターン**: 4種類のカラースキーム（デフォルト、アルファブレンド、グラデーション）

---

## インストールと実行

ローカル環境で実行する場合は、以下の手順に従ってください。

### 前提条件
* **Python 3.9 以上**を推奨
* `pip` パッケージマネージャーがインストールされていること

### セットアップ手順

#### 1. リポジトリのクローン
```bash
git clone https://github.com/YourUsername/IR-Analyzer.git
cd IR-Analyzer
```

#### 2. 仮想環境の作成（推奨）
プロジェクトごとに独立した環境を構築することで、依存関係の競合を防ぎます。

```bash
python -m venv venv

# Windows の場合
.\venv\Scripts\activate

# Mac / Linux の場合
source venv/bin/activate
```

#### 3. 依存ライブラリのインストール
```bash
pip install -r requirements.txt
```

以下のライブラリがインストールされます：
* `streamlit>=1.28.0` - Webアプリケーションフレームワーク
* `numpy>=1.24.0` - 数値計算
* `matplotlib>=3.7.0` - グラフ描画
* `scipy>=1.11.0` - 信号処理

#### 4. アプリケーションの起動
```bash
streamlit run app.py
```

ブラウザが自動的に開き、アプリケーションが表示されます。デフォルトでは `http://localhost:8501` でアクセス可能です。

---

## 使い方

### 基本的なワークフロー

#### 1. ファイルのアップロード
* 左側のサイドバー、またはメイン画面のドラッグ&ドロップエリアに**WAV形式のIRファイル**をアップロードします
* 最大8ファイルまで同時比較が可能です（各ファイル200MBまで）
* 対応フォーマット: モノラル / ステレオ WAV（ステレオの場合は自動的にモノラルへ変換されます）

#### 2. 波形表示の設定
サイドバーの "Waveform Settings" セクションで以下を調整します：

* **Waveform Display Duration**:
    * 時間軸の表示範囲（1～100ms）
    * 初期反射を詳細に観察する場合は短く、全体的な減衰カーブを見る場合は長く設定します
    
* **Align Waveform Peaks**:
    * 有効にすると、全波形のピーク位置が時間軸上で揃います
    * 異なる録音タイミングのIRを位相比較する際に使用します
    
* **Waveform Normalization Mode**:
    * **Relative（推奨）**: 各ファイルを個別に規格化。波形形状の比較に適する
    * **Absolute**: 全ファイルを共通基準で規格化。音量差を保持

#### 3. FFT解析の設定
サイドバーの "FFT Settings" および "Advanced Settings" で以下を調整します：

* **FFT Analysis Duration**:
    * FFT解析に使用するサンプル長（1～1000ms）
    * 周波数分解能に直接影響します（長いほど高分解能）
    * 例: 100ms → 10 Hz分解能、1000ms → 1 Hz分解能
    
* **FFT Window Size (Minimum)**:
    * 最小FFTサイズ（2048～65536）
    * Duration設定から自動計算されるFFTサイズがこの値を下回る場合、ここで指定した値が使用されます
    
* **FFT Window Function**:
    * 用途に応じて選択してください
    * **一般的な解析**: Hann または Blackman-Harris
    * **高精度測定**: Kaiser β=14.0
    * **ゲイン測定**: Flat Top
    
* **Data Shift Mode**:
    * **Auto（推奨）**: Bell型窓関数使用時のみ自動シフト
    * **None**: シフトなし（矩形窓使用時に推奨）
    * **Peak Centering**: ピーク位置を自動検出してシフト（最高精度）
    
* **FFT Smoothing**:
    * 周波数応答がギザギザして見にくい場合は、`1/24 Octave` 程度に設定してください
    * より滑らかな表示が必要な場合は `1/6 Octave` ～ `1 Octave`を選択してください（ただし、結果が出力されるまで時間がかかります）
    
* **FFT Normalization Mode**:
    * **Relative（デフォルト）**: 各ファイルを個別に0 dB基準へ規格化
    * **Absolute**: 全ファイルを共通の最大値で規格化（音量差を保持）

#### 4. グラフのエクスポート
各グラフの下部にある "Download High-Resolution..." ボタンから画像を保存します。

---

## 使用ライブラリ

本ツールは以下のPythonライブラリを使用しています：

* [**Streamlit**](https://streamlit.io/) `>=1.28.0`
    * Webアプリケーションフレームワーク
    * 迅速なプロトタイピングとUIの構築に使用
    
* [**NumPy**](https://numpy.org/) `>=1.24.0`
    * 高速な数値計算ライブラリ
    * 配列操作、FFT、統計処理に使用
    
* [**Matplotlib**](https://matplotlib.org/) `>=3.7.0`
    * グラフ描画ライブラリ
    * 高品質な科学技術グラフの生成に使用
    
* [**SciPy**](https://scipy.org/) `>=1.11.0`
    * 科学技術計算ライブラリ
    * 窓関数の生成、信号処理アルゴリズムに使用

すべての依存関係は `requirements.txt` に記載されています。

---

## 開発者情報

* **Name**: 山本隼大（Hayata Yamamoto）
* **技術ブログ**: [https://publish.obsidian.md/mjm-12](https://publish.obsidian.md/mjm-12)
* **GitHub**: [https://github.com/YourUsername](https://github.com/YourUsername)

---

## ライセンス 

本プロジェクトは **MIT License** の下で公開されています。詳細は [LICENSE](LICENSE) ファイルをご確認ください。

